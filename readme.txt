in this folder, 

1_data_preprocess.sh contains data pre-process related code, 2_Kmeans_cluster.R contains k-means clustering related code.

./ref contains some refecrence genome
./res have some intermediate output which can be used as control for code usage.
also, there is a `mm10.vM23.tss.bed.gz` file in `res` directory, which is from Gencode vM23, only kept protein coding genes with annotation level 1 or 2.

